import styled from "styled-components";
import { animated } from "react-spring";

export const TextWrapper = styled.div`
  font-size: 49px;
  display: flex;
  flex-direction: row;
`;

export const TextTrails = styled(animated.div)`
  padding: 3px;
`;
